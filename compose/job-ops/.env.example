# Optional: the Gmail integration is off unless both are supplied. Nothing
# else in this stack needs an encrypted input, so secrets/compose/
# job-ops.sops.env is only required if you enable that feature.
#GMAIL_OAUTH_CLIENT_ID=replace-me
#GMAIL_OAUTH_CLIENT_SECRET=replace-me
