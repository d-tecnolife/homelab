# Generate each value independently. Keep the real values only in
# secrets/compose/authentik.sops.env.
PG_PASS=replace-with-a-random-postgresql-password
AUTHENTIK_SECRET_KEY=replace-with-a-random-authentik-secret-key
