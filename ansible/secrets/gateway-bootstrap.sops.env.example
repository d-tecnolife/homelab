# Encrypt a copy of this file. The renderer derives a bcrypt hash locally; the
# generated config contains only that hash and the supplied public SSH key.
GATEWAY_ROOT_PASSWORD=replace-with-a-long-unique-password
